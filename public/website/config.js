/* ============================================================
   VEOX EDITS – WEBSITE-EINSTELLUNGEN (hier alles selbst ändern!)
   ------------------------------------------------------------
   So änderst du es später SELBST (ohne jemanden fragen zu müssen):
   1. Auf github.com dein Repo öffnen → Ordner: public/website/
   2. Auf diese Datei "config.js" klicken → oben rechts auf den Stift (✏️ Edit)
   3. Werte zwischen den Anführungszeichen ändern
   4. Unten "Commit changes" klicken → nach ~1 Minute ist es live.

   Leer lassen ("") = es wird der Standardwert / kein Button verwendet.
   ============================================================ */
window.VEOX_CONFIG = {
  // --- Marke ---
  brand: 'Veox Edits',
  slogan: 'Content, der den Daumen stoppt.',

  // --- Kontakt (Buttons + Impressum ziehen sich das automatisch) ---
  email: 'u7135627@gmx.de',
  instagram: 'veox.edits',     // ohne @
  tiktok: 'veox.edits1',       // ohne @

  // --- Impressum (in Deutschland Pflicht) ---
  impressumName: 'Mailo Heller',
  impressumStreet: 'Lohgerberstraße 8',
  impressumCity: '',           // z.B. "12345 Musterstadt"  <-- bitte noch ergänzen
  impressumTaxId: '',          // Steuernr./USt-IdNr. (falls vorhanden)
};
